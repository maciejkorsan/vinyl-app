<h1 class="text-4xl font-bold sr-only">Dashboard</h1>
<div class="my-24">
  <p class="mb-8 text-4xl text-center text-gray-300">Your collection has
    <span class="font-bold text-white"><span id="records_count"><%= @records_count %></span> unique records.</span>
    <br/>
    This year, you’ve played records <span class="font-bold text-white"><span id="logs_count"><%= @logs_count %></span> times</span>, 
    <br/>
   spending a total of  <br/>
    <span class="font-bold text-white"><span id="total_time_count"><%= format_time(@total_time_listened) %></span></span>listening.</p>
</div>
<div class="grid grid-cols-2 gap-4">
  <div class="p-4 bg-gray-800 rounded-md">
    <%= render "last_played"%>
  </div>
  <div class="p-4 bg-gray-800 rounded-md">
    <%= render "log_box" %>
  </div>
</div>
